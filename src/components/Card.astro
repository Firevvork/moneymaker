---
interface Props {
  emoji: string;
  title: string;
}

const { emoji, title } = Astro.props;
---

<div class="font-mono w-40 flex flex-col gap-1">
  <div class="text-3xl cursor-default">{emoji}</div>
  <a href="a" class="hover:text-blue-700 hover:font-bold">{title}</a>
</div>
